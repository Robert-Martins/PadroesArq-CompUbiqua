import { Text } from "react-native";
import { Flex } from "@/core/components";

const Home: React.FC = () => {
    return (
        <Flex>
            <Text> Home</Text>
        </Flex>
    )
}

export default Home;